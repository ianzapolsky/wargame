'use strict';
var now = require('./now').default;
var parse = require('./parse').default;
var since = function (time) {
  return now() - time;
};
exports.now = now, exports.parse = parse, exports.since = since;