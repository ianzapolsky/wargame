'use strict';
var iteratorOf = require('./get').default;
exports.default = function forOf(object, fn, ctx) {
  var iterator = iteratorOf(object), step;
  while (!(step = iterator.next()).done) {
    var result = fn.call(ctx, step.value, object);
    if (result === false) {
      break;
    }
  }
  return object;
};