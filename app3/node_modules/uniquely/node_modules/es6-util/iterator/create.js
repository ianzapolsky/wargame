'use strict';
var getIteratorSymbol = require('./symbol').default;
exports.default = function createIterator(next) {
  var it = { next: next };
  it[getIteratorSymbol()] = function () {
    return it;
  };
  return it;
};