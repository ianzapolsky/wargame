'use strict';
var callExpressionA = require('./array');
var arrayValues = callExpressionA.values, arrayKeys = callExpressionA.keys, arrayEntries = callExpressionA.entries;
var callExpression = require('./object');
var objectValues = callExpression.values, objectKeys = callExpression.keys, objectEntries = callExpression.entries;
var values = function (object) {
  return object instanceof Array ? arrayValues(object) : objectValues(object);
};
exports.values = values;
var keys = function (object) {
  return object instanceof Array ? arrayKeys(object) : objectKeys(object);
};
exports.keys = keys;
var entries = function (object) {
  return object instanceof Array ? arrayEntries(object) : objectEntries(object);
};
exports.entries = entries;